# SUPPLY LINE TRAINER

エイム訓練で補給を稼ぎ、自動戦闘中の部隊を強化する小規模3Dゲームです。

![射撃訓練](docs/images/crosshair.png)

## 起動

[Releases](../../releases) からWindows版ZIPをダウンロードして展開し、`SupplyLineTrainer.exe` を起動してください。同じフォルダーの `SupplyLineTrainer.pck` が必要です。Windows 10/11 64bit向けです。

## 操作

- WASD: 移動
- マウス: 視点
- 左クリック: 射撃
- Esc: 一時停止／設定

準備画面で武器・難易度・30秒／60秒を選び、「訓練開始」を押します。選択中の項目はチェック表示され、未開放武器には開放条件が表示されます。ハンドガンは単発、オートライフルは長押し連射、バーストライフルは1クリック3発です。訓練中のEscメニューから、その時点の成果を精算して途中終了できます。

「部隊画面」では、左側で増員・装備強化・収入設備を購入し、右側で部隊の進軍を確認できます。購入数は×1／×10／×100から選択できます。3スロットの指揮官装備は、各スロットで戦闘寄りか収入寄りを選べます。購入済み装備の切り替えは無料です。訓練中のEscメニューには部隊への導線を置かず、準備画面または結果画面から移動します。

| 初期部隊 | 成長後の部隊 |
| --- | --- |
| ![初期部隊](docs/images/army_initial.png) | ![成長後の部隊](docs/images/army_advanced.png) |

設定画面はCS:GO／VALORANT／Apex Legends基準の腰だめ感度、DPIとcm/360、4:3基準FOV、4種類のクロスヘア、色・サイズ・補助輪郭、音量に対応しています。「適用」「変更を取り消す」「初期値に戻す」は進行データへ影響しません。VALORANTとApex Legendsの係数は互換用候補です。

![設定画面](docs/images/settings.png)

## 開発環境

- Godot Engine 4.7.2 stable
- GDScript
- GL Compatibility renderer
- 外部アセット／プラグインなし

プロジェクト編集時は `project.godot` をGodot 4.7.2で開いてください。主要な調整値は `balance.gd` に集約しています。

## 検証

実施済みの検証内容と未確認事項は [VERIFY.md](VERIFY.md) に記載しています。テスト用スクリプトはゲーム本体の書き出し対象から除外されています。

## 保存

進行、時間別の自己ベスト、設定、指揮官装備はGodotのユーザーデータ領域に `save_v1.json` として自動保存されます。旧形式は初回読込時に `save_v1.backup.json` へ退避してから移行し、旧採点記録も別枠で保持します。破損時は安全な初期値で起動します。オフラインクレジットは最大8時間、金額は戦線ごとの上限までです。

戦線は旧最終戦線の後も継続します。増員に応じて、ピクセル兵士の隊列から街・塔・地球・太陽系・銀河を基準にした抽象的な軍勢表示へ段階的に切り替わります。クレジットは10進文字列で保存し、巨大値の比較・加減算・購入判定で整数精度を維持します。

## 既知の制約

- Godot 4.7.2の `InputEventMouseMotion.screen_relative` を優先し、利用できないイベントでは `relative` を使用します。入力差分へdeltaTimeやDPIは掛けませんが、物理マウスカウントとの完全一致は実機比較が必要です。
- UIは論理Viewportとアンカーを基準に追従します。Windows表示倍率100%以外での実機操作は未確認です。
- 感度一致は同じDPIでのcm/360を対象とし、FOVの見かけ、ADS、反動、入力遅延は再現対象外です。
- 巨大軍勢は実人口や物理配置の再現ではなく、規模を伝える段階表現です。戦闘力・敵耐久など連続量はInfinityを避ける上限付きfloat、クレジットは必要な比較・加減算に絞った大数表現です。
- 実機での長時間プレイによる経済バランスは、計算シミュレーションで調整した目安です。
