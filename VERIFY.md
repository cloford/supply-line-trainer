# 確認報告

## 実行して確認

- Godot 4.7.2 stableで全スクリプトのコンパイルとリソース読み込みが成功。
- ヘッドレス起動を3秒間実行し、起動時エラー・未処理例外なし。
- 自動テストでFOV変換（4:3水平90度 → 垂直73.7398度）、3武器×3難易度の正報酬、価格増加、強化効果、オフライン上限、初期開放状態を確認。
- 1秒刻みの進行シミュレーションで、標準報酬105/分の想定時にオートライフル約3.4分、バーストライフル約12.5分、最終突破約35.5分。これは実プレイテストではない。
- Windows Desktop release書き出しが成功し、実行ファイルとPCKを生成。生成したEXEをヘッドレスで3秒間起動し、実行エラーなし。
- 1280×720、1920×1080、1024×768相当のViewportで設定・結果カードが表示領域内に収まることを自動検査。
- Escのキーリピート抑止、訓練／部隊画面の往復、フォーカス喪失時の停止、復帰時の射撃抑止を自動検査。
- 実レンダリングで設定画面、通常／黒色クロスヘア、部隊の初期／上位外見を画像確認。
- 隔離したユーザーデータ領域でversion 1セーブの進行・黒色クロスヘア設定を保存／再読込し、破損JSONから初期値へ復旧することを確認。

## 実装をコード／自動試験で確認

- 射撃・タイマー・追従採点・部隊進行はdelta時間または固定レート基準。
- カメラ中心のレイ判定、壁による遮蔽、プレイヤー自身を射線判定から除外。
- フォーカス喪失と設定画面で訓練を停止し、押下状態を解除。再開時はクリック解放まで射撃を抑止。
- 報酬確定ガード、途中終了の自己ベスト除外、撃破済みバースト標的の再加算防止。
- 原子的な一時ファイル保存、破損セーブの初期値復帰、オフライン時間／金額上限、時計巻き戻り0秒。
- 最終戦線後も準備画面と訓練を継続可能。

## 未確認

- 人が実際に操作するWindows GUIプレイの手触り、Windows表示倍率100%以外、各マウス機種での物理カウント比較。
- 長時間の実プレイによる経済バランスと実測FPS。
- Windows 10/11の別PCでの起動互換性。
