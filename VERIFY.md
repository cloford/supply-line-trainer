# 確認報告

## 実行して確認

- Godot 4.7.2 stableで全スクリプトのコンパイルとリソース読み込みが成功。
- 自動テストでFOV変換、3武器×3難易度の新採点、ボーナス45%上限、30／60秒の選択、感度換算、無限戦線、成長段階を確認。
- 1280×720、1920×1080、1024×768相当のViewportで一時停止・設定・結果カードが表示領域内に収まり、クロスヘア中心がHUD中心と一致することを自動検査。
- 設定の適用前編集・取り消し・初期値ドラフト、Escキーリピート抑止、訓練／部隊画面の往復、フォーカス喪失、復帰時の射撃抑止を自動検査。
- 途中終了クレジットの比例精算、自己ベスト非更新、報酬確定の一回限り、再挑戦時の即時初期化を自動検査。
- オート標的が開始時に飛ばないこと、バースト標的が直前位置から一定距離を取ること、表示兵士上限を自動検査。
- 指揮官装備の購入・装備反映と、黒色クロスヘアを含む設定反映を自動検査。
- 10^18を越えるクレジットの比較・加算・減算と、`1000000000000000000000000000001` の保存再読込一致を確認。
- 隔離したユーザーデータ領域でversion 1セーブの進行・所持額・旧記録・黒色クロスヘアを移行し、旧ファイルのバックアップ作成と破損JSONからの初期値復旧を確認。
- 1秒刻みの進行シミュレーションで、標準報酬想定時にオートライフル開放約3.4分、バーストライフル開放約12.5分、戦線20到達約120分。これは実プレイテストではない。
- 実レンダリングで設定画面、通常／黒色クロスヘア、部隊の初期／100京級表示を画像確認。
- Windows Desktop release書き出しと、生成EXEの起動確認を実施。

## 実装をコード／自動試験で確認

- 射撃・タイマー・追従採点・部隊進行はdelta時間または固定レート基準。
- カメラ中心のレイ判定、壁による遮蔽、プレイヤー自身を射線判定から除外。
- フォーカス喪失と設定画面で訓練を停止し、押下状態を解除。再開時はクリック解放まで射撃を抑止。
- 報酬確定ガード、途中終了の自己ベスト除外、時間を含む採点キー、撃破済みバースト標的の再加算防止。
- 一時ファイル保存、旧セーブの初回バックアップ、破損セーブの初期値復帰、オフライン時間／金額上限、時計巻き戻り0秒。
- 旧最終戦線後も戦線名・耐久・報酬を生成し、準備画面と訓練を継続可能。
- 部隊の成長状態と描画を分離し、表示は最大48兵士または点群へ抽象化。背景切替で進行値は初期化しない。

## 未確認

- 人が実際に操作するWindows GUIプレイの手触り、Windows表示倍率100%以外、CS:GO／VALORANT／Apex Legendsとの物理マウス距離比較。
- 長時間の実プレイによる経済バランスと実測FPS。
- 通常プレイだけで100京級へ到達する所要時間。100京級の画面確認は開発用状態注入で実施。
- Windows 10/11の別PCでの起動互換性。
